import { BindToDirective } from './bind-to.directive';

describe('BindToDirective', () => {
  it('should create an instance', () => {
    const directive = new BindToDirective();
    expect(directive).toBeTruthy();
  });
});
